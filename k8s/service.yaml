apiVersion: v1
kind: Service
metadata:
  name: hello-pipecd
spec:
  type: NodePort
  selector:
    app: hello-pipecd
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080